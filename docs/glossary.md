# Glossary

| Term | Meaning |
|---|---|
| MCP | Model Context Protocol. |
| Stdio | Local process transport over standard input/output. |
| Streamable HTTP | Current remote MCP HTTP transport. |
| Upstream boundary | The adapter layer that normalizes `notebooklm-py` responses. |
