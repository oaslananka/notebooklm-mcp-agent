# Release Development

Release Please prepares version and changelog pull requests from Conventional Commits. Publishing is separate and owner-triggered through the `Publish` workflow.

Local release readiness:

```bash
task verify:release
```

```powershell
task verify:release
```
