# OpenSSF Scorecard

The `OpenSSF Scorecard` workflow runs on the default branch and uploads SARIF results when repository permissions allow it.

Known score constraints for a new repository:

- Maintained history and review depth improve after normal project activity.
- Branch protection must be configured by an owner token with repository administration permission.
- Signed release evidence exists only after the owner triggers a release.
